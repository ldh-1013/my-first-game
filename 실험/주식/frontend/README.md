# Frontend

React, TypeScript, Vite, Tailwind CSS, Recharts 기반 로컬 대시보드입니다.

## 실행

```powershell
npm install
npm run dev
```

기본 API 주소는 `http://localhost:8000`입니다. 변경하려면 다음 환경 변수를 사용합니다.

```env
VITE_API_URL=http://localhost:8000
```

## 빌드

```powershell
npm run build
```

